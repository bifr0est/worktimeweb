[project]
name = "worktimeweb"
version = "0.1.0"
description = "Simple Flask app to calculate work end times."
requires-python = ">=3.13"
dependencies = [
    "flask==3.1.3",
    "gunicorn==26.0.0",
    "python-dotenv==1.2.2",
    "pytz==2026.2",
]

[tool.uv]
package = false
